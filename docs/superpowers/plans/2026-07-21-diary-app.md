# 일기장 웹앱 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 검색과 회고(특히 7일 전/1개월 전/N년 전 계단식 회고)에 초점을 맞춘 개인용 로컬 일기장 웹앱을 만든다.

**Architecture:** C# Blazor Server(전역 InteractiveServer 렌더링) 앱이 EF Core로 SQLite 파일(`diary.db`) 하나에 접근한다. 도메인 로직은 `Services/` 아래 4개의 얇은 서비스(작성/조회/삭제, 회고 계산, 검색, 이미지 저장)로 분리하고, Razor 페이지는 이 서비스들을 DI로 주입받아 화면만 담당한다.

**Tech Stack:** .NET 10 / C# / ASP.NET Core Blazor Server / EF Core 10 (Sqlite provider) / xUnit

## Global Constraints

- 대상 프레임워크: net10.0 (설치된 .NET SDK 10.0.201 기준)
- UI 렌더링: Blazor Server, 전역 InteractiveServer (프로젝트 생성 시 `-ai`/`--all-interactive` 옵션으로 설정 — 페이지마다 `@rendermode`를 따로 붙일 필요 없음)
- DB: SQLite 단일 파일 `diary.db`. 스키마 생성은 `Database.EnsureCreated()`로 처리하고 별도 EF 마이그레이션 도구는 쓰지 않는다 (개인용 단일 파일 앱이라 마이그레이션 관리 자체가 불필요 — YAGNI)
- 일기 항목: 제목 없음, 날짜로만 구분. 하루에 여러 개 작성 가능 (Date 컬럼에 유니크 제약 없음)
- 이미지: 장당 5MB 제한, 허용 확장자 `.jpg/.jpeg/.png/.gif/.webp`, `wwwroot/uploads/`에 저장하고 DB에는 상대 경로만 기록
- 태그 다중 필터: OR 로직
- 회고(홈 화면): 오늘 기준 **7일 전 / 1개월 전 / N년 전(있는 만큼)** 계단식으로 표시하고, 데이터 없는 tier는 표시하지 않음
- 검색·목록 화면은 하나로 결합 (`/timeline`) — 별도 검색 전용 화면을 두지 않음 (스펙의 미해결 사항에 대한 구현 단계 결정)

---

## Task 1: 프로젝트 스캐폴딩

**Files:**
- Create: `DiaryApp.slnx`
- Create: `src/DiaryApp/` (Blazor Server 프로젝트 전체 — `dotnet new`로 생성)
- Create: `tests/DiaryApp.Tests/` (xUnit 프로젝트 전체 — `dotnet new`로 생성)
- Create: `.gitignore`

**Interfaces:**
- Consumes: 없음 (첫 작업)
- Produces: `src/DiaryApp/DiaryApp.csproj` (프로젝트 이름 `DiaryApp`, 네임스페이스 `DiaryApp`), `tests/DiaryApp.Tests/DiaryApp.Tests.csproj` (프로젝트 참조로 `DiaryApp` 포함). 이후 모든 작업은 이 두 프로젝트 아래에 파일을 추가한다.

- [ ] **Step 1: 솔루션과 프로젝트 생성**

작업 디렉터리: `C:\claude\일기장`

```bash
dotnet new sln -n DiaryApp
dotnet new blazor -n DiaryApp -o src/DiaryApp -int Server -ai -e
dotnet new xunit -n DiaryApp.Tests -o tests/DiaryApp.Tests
```

주의: `dotnet new sln`은 이 SDK 버전(10.0.201)에서 `DiaryApp.sln`이 아니라 **`DiaryApp.slnx`**를 생성한다.

- [ ] **Step 2: 솔루션에 프로젝트 추가 및 프로젝트 참조 연결**

```bash
dotnet sln DiaryApp.slnx add src/DiaryApp/DiaryApp.csproj tests/DiaryApp.Tests/DiaryApp.Tests.csproj
dotnet add tests/DiaryApp.Tests/DiaryApp.Tests.csproj reference src/DiaryApp/DiaryApp.csproj
```

- [ ] **Step 3: 필요한 NuGet 패키지 설치**

```bash
dotnet add src/DiaryApp/DiaryApp.csproj package Microsoft.EntityFrameworkCore.Sqlite
dotnet add src/DiaryApp/DiaryApp.csproj package Microsoft.EntityFrameworkCore.Design
dotnet add tests/DiaryApp.Tests/DiaryApp.Tests.csproj package Microsoft.EntityFrameworkCore.Sqlite
```

- [ ] **Step 4: `.gitignore` 작성**

`C:\claude\일기장\.gitignore` 파일을 만든다 (이미 있는 `CLAUDE.md`, `docs/`와 같은 위치):

```gitignore
bin/
obj/
src/DiaryApp/diary.db
src/DiaryApp/wwwroot/uploads/*
!src/DiaryApp/wwwroot/uploads/.gitkeep
```

`src/DiaryApp/wwwroot/uploads/.gitkeep` 빈 파일도 함께 만들어 폴더 자체는 git에 남긴다.

- [ ] **Step 5: 빌드 확인**

```bash
dotnet build DiaryApp.slnx
```

Expected: `빌드했습니다.` / `오류 0개` (경고는 있어도 무방 — SQLitePCLRaw 관련 NU1903 경고는 알려진 이슈이며 이 프로젝트 범위에서 조치하지 않는다)

- [ ] **Step 6: 커밋**

```bash
git add DiaryApp.slnx src/ tests/ .gitignore
git commit -m "Scaffold Blazor Server + xUnit project structure"
```

---

## Task 2: 데이터 모델 + DbContext

**Files:**
- Create: `src/DiaryApp/Models/DiaryEntry.cs`
- Create: `src/DiaryApp/Models/Tag.cs`
- Create: `src/DiaryApp/Models/DiaryImage.cs`
- Create: `src/DiaryApp/Data/DiaryDbContext.cs`
- Create: `tests/DiaryApp.Tests/TestDb.cs`
- Test: `tests/DiaryApp.Tests/DiaryDbContextTests.cs`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `DiaryApp.Models.DiaryEntry { int Id; DateTime Date; string Content; int? Mood; DateTime CreatedAt; List<Tag> Tags; List<DiaryImage> Images; }`
  - `DiaryApp.Models.Tag { int Id; string Name; List<DiaryEntry> Entries; }`
  - `DiaryApp.Models.DiaryImage { int Id; int DiaryEntryId; DiaryEntry? DiaryEntry; string FilePath; }`
  - `DiaryApp.Data.DiaryDbContext(DbContextOptions<DiaryDbContext> options)` — `DbSet<DiaryEntry> DiaryEntries`, `DbSet<Tag> Tags`, `DbSet<DiaryImage> DiaryImages`
  - `DiaryApp.Tests.TestDb : IDisposable` — `.Context`(DiaryDbContext) 프로퍼티 제공. **모든 이후 테스트 작업이 이 헬퍼를 재사용한다.**

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/DiaryApp.Tests/TestDb.cs`:

```csharp
namespace DiaryApp.Tests;

using DiaryApp.Data;
using Microsoft.Data.Sqlite;
using Microsoft.EntityFrameworkCore;

public sealed class TestDb : IDisposable
{
    public DiaryDbContext Context { get; }
    private readonly SqliteConnection _connection;

    public TestDb()
    {
        _connection = new SqliteConnection("Filename=:memory:");
        _connection.Open();
        var options = new DbContextOptionsBuilder<DiaryDbContext>()
            .UseSqlite(_connection)
            .Options;
        Context = new DiaryDbContext(options);
        Context.Database.EnsureCreated();
    }

    public void Dispose()
    {
        Context.Dispose();
        _connection.Dispose();
    }
}
```

`tests/DiaryApp.Tests/DiaryDbContextTests.cs`:

```csharp
namespace DiaryApp.Tests;

using DiaryApp.Models;
using Microsoft.EntityFrameworkCore;
using Xunit;

public class DiaryDbContextTests
{
    [Fact]
    public async Task SavesAndReadsEntryWithTagsAndImages()
    {
        using var db = new TestDb();

        var entry = new DiaryEntry
        {
            Date = new DateTime(2026, 7, 20),
            Content = "테스트 일기 본문",
            Mood = 4,
            CreatedAt = DateTime.Now,
        };
        entry.Tags.Add(new Tag { Name = "일상" });
        entry.Images.Add(new DiaryImage { FilePath = "uploads/test.jpg" });

        db.Context.DiaryEntries.Add(entry);
        await db.Context.SaveChangesAsync();

        var reloaded = await db.Context.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .SingleAsync(e => e.Id == entry.Id);

        Assert.Equal("테스트 일기 본문", reloaded.Content);
        Assert.Equal(4, reloaded.Mood);
        Assert.Single(reloaded.Tags);
        Assert.Equal("일상", reloaded.Tags[0].Name);
        Assert.Single(reloaded.Images);
    }

    [Fact]
    public async Task TagNameIsUnique()
    {
        using var db = new TestDb();

        db.Context.Tags.Add(new Tag { Name = "행복" });
        await db.Context.SaveChangesAsync();

        db.Context.Tags.Add(new Tag { Name = "행복" });

        await Assert.ThrowsAnyAsync<DbUpdateException>(() => db.Context.SaveChangesAsync());
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter DiaryDbContextTests
```

Expected: 컴파일 에러 (`DiaryApp.Models`, `DiaryApp.Data.DiaryDbContext` 네임스페이스/타입이 존재하지 않음)

- [ ] **Step 3: 모델과 DbContext 구현**

`src/DiaryApp/Models/DiaryEntry.cs`:

```csharp
namespace DiaryApp.Models;

public class DiaryEntry
{
    public int Id { get; set; }
    public DateTime Date { get; set; }
    public string Content { get; set; } = string.Empty;
    public int? Mood { get; set; }
    public DateTime CreatedAt { get; set; }
    public List<Tag> Tags { get; set; } = new();
    public List<DiaryImage> Images { get; set; } = new();
}
```

`src/DiaryApp/Models/Tag.cs`:

```csharp
namespace DiaryApp.Models;

public class Tag
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public List<DiaryEntry> Entries { get; set; } = new();
}
```

`src/DiaryApp/Models/DiaryImage.cs`:

```csharp
namespace DiaryApp.Models;

public class DiaryImage
{
    public int Id { get; set; }
    public int DiaryEntryId { get; set; }
    public DiaryEntry? DiaryEntry { get; set; }
    public string FilePath { get; set; } = string.Empty;
}
```

`src/DiaryApp/Data/DiaryDbContext.cs`:

```csharp
namespace DiaryApp.Data;

using DiaryApp.Models;
using Microsoft.EntityFrameworkCore;

public class DiaryDbContext : DbContext
{
    public DiaryDbContext(DbContextOptions<DiaryDbContext> options) : base(options) { }

    public DbSet<DiaryEntry> DiaryEntries => Set<DiaryEntry>();
    public DbSet<Tag> Tags => Set<Tag>();
    public DbSet<DiaryImage> DiaryImages => Set<DiaryImage>();

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Tag>()
            .HasIndex(t => t.Name)
            .IsUnique();

        modelBuilder.Entity<DiaryEntry>()
            .HasMany(e => e.Tags)
            .WithMany(t => t.Entries);

        modelBuilder.Entity<DiaryImage>()
            .HasOne(i => i.DiaryEntry)
            .WithMany(e => e.Images)
            .HasForeignKey(i => i.DiaryEntryId);
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter DiaryDbContextTests
```

Expected: `통과!` / 2개 테스트 성공

- [ ] **Step 5: 커밋**

```bash
git add src/DiaryApp/Models src/DiaryApp/Data tests/DiaryApp.Tests/TestDb.cs tests/DiaryApp.Tests/DiaryDbContextTests.cs
git commit -m "Add diary data models and EF Core DbContext"
```

---

## Task 3: DiaryEntryService (작성/조회/삭제)

**Files:**
- Create: `src/DiaryApp/Services/DiaryEntryService.cs`
- Test: `tests/DiaryApp.Tests/DiaryEntryServiceTests.cs`

**Interfaces:**
- Consumes: `DiaryDbContext`(Task 2), `DiaryEntry`/`Tag`/`DiaryImage`(Task 2), `TestDb`(Task 2)
- Produces: `DiaryApp.Services.DiaryEntryService(DiaryDbContext db)` —
  - `Task<DiaryEntry> CreateAsync(DateTime date, string content, int? mood, List<string> tagNames)`
  - `Task<List<DiaryEntry>> GetAllDescendingAsync()`
  - `Task<DiaryEntry?> GetByIdAsync(int id)`
  - `Task AddImageAsync(int entryId, string filePath)`
  - 이후 Task 4/5는 `DiaryEntryService`로 테스트 데이터를 만들고, Task 9(새 일기 작성 화면)는 `CreateAsync`/`AddImageAsync`를, Task 10(목록 화면)은 `GetAllDescendingAsync`를 사용한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/DiaryApp.Tests/DiaryEntryServiceTests.cs`:

```csharp
namespace DiaryApp.Tests;

using DiaryApp.Services;
using Microsoft.EntityFrameworkCore;
using Xunit;

public class DiaryEntryServiceTests
{
    [Fact]
    public async Task CreateAsync_SavesEntryWithNewTags()
    {
        using var db = new TestDb();
        var service = new DiaryEntryService(db.Context);

        var entry = await service.CreateAsync(new DateTime(2026, 7, 20), "오늘은 날씨가 좋았다", 5, new List<string> { "행복", "일상" });

        Assert.NotEqual(0, entry.Id);
        Assert.Equal(2, entry.Tags.Count);
    }

    [Fact]
    public async Task CreateAsync_ReusesExistingTag()
    {
        using var db = new TestDb();
        var service = new DiaryEntryService(db.Context);

        await service.CreateAsync(new DateTime(2026, 7, 19), "첫 일기", null, new List<string> { "일상" });
        await service.CreateAsync(new DateTime(2026, 7, 20), "둘째 일기", null, new List<string> { "일상" });

        Assert.Equal(1, await db.Context.Tags.CountAsync());
    }

    [Fact]
    public async Task CreateAsync_ThrowsWhenContentIsEmpty()
    {
        using var db = new TestDb();
        var service = new DiaryEntryService(db.Context);

        await Assert.ThrowsAsync<ArgumentException>(
            () => service.CreateAsync(new DateTime(2026, 7, 20), "  ", null, new List<string>()));
    }

    [Fact]
    public async Task GetAllDescendingAsync_OrdersByDateDescending()
    {
        using var db = new TestDb();
        var service = new DiaryEntryService(db.Context);

        await service.CreateAsync(new DateTime(2026, 7, 18), "오래된 글", null, new List<string>());
        await service.CreateAsync(new DateTime(2026, 7, 20), "최신 글", null, new List<string>());

        var all = await service.GetAllDescendingAsync();

        Assert.Equal("최신 글", all[0].Content);
        Assert.Equal("오래된 글", all[1].Content);
    }

    [Fact]
    public async Task AddImageAsync_AttachesImageToExistingEntry()
    {
        using var db = new TestDb();
        var service = new DiaryEntryService(db.Context);
        var entry = await service.CreateAsync(new DateTime(2026, 7, 20), "사진이 있는 글", null, new List<string>());

        await service.AddImageAsync(entry.Id, "uploads/abc.jpg");

        var reloaded = await service.GetByIdAsync(entry.Id);
        Assert.NotNull(reloaded);
        Assert.Single(reloaded!.Images);
        Assert.Equal("uploads/abc.jpg", reloaded.Images[0].FilePath);
    }

    [Fact]
    public async Task AddImageAsync_ThrowsWhenEntryDoesNotExist()
    {
        using var db = new TestDb();
        var service = new DiaryEntryService(db.Context);

        await Assert.ThrowsAsync<InvalidOperationException>(() => service.AddImageAsync(999, "uploads/x.jpg"));
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter DiaryEntryServiceTests
```

Expected: 컴파일 에러 (`DiaryApp.Services.DiaryEntryService` 타입 없음)

- [ ] **Step 3: 구현**

`src/DiaryApp/Services/DiaryEntryService.cs`:

```csharp
namespace DiaryApp.Services;

using DiaryApp.Data;
using DiaryApp.Models;
using Microsoft.EntityFrameworkCore;

public class DiaryEntryService
{
    private readonly DiaryDbContext _db;

    public DiaryEntryService(DiaryDbContext db)
    {
        _db = db;
    }

    public async Task<DiaryEntry> CreateAsync(DateTime date, string content, int? mood, List<string> tagNames)
    {
        if (string.IsNullOrWhiteSpace(content))
            throw new ArgumentException("본문은 비어 있을 수 없습니다.", nameof(content));

        var entry = new DiaryEntry
        {
            Date = date,
            Content = content,
            Mood = mood,
            CreatedAt = DateTime.Now,
        };

        foreach (var name in tagNames.Select(t => t.Trim()).Where(t => t.Length > 0).Distinct())
        {
            var tag = await _db.Tags.FirstOrDefaultAsync(t => t.Name == name);
            if (tag == null)
            {
                tag = new Tag { Name = name };
                _db.Tags.Add(tag);
            }
            entry.Tags.Add(tag);
        }

        _db.DiaryEntries.Add(entry);
        await _db.SaveChangesAsync();
        return entry;
    }

    public async Task<List<DiaryEntry>> GetAllDescendingAsync()
    {
        return await _db.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .OrderByDescending(e => e.Date)
            .ThenByDescending(e => e.CreatedAt)
            .ToListAsync();
    }

    public async Task<DiaryEntry?> GetByIdAsync(int id)
    {
        return await _db.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .FirstOrDefaultAsync(e => e.Id == id);
    }

    public async Task AddImageAsync(int entryId, string filePath)
    {
        var entry = await _db.DiaryEntries.FindAsync(entryId);
        if (entry == null)
            throw new InvalidOperationException($"Id {entryId}에 해당하는 일기 항목이 없습니다.");
        _db.DiaryImages.Add(new DiaryImage { DiaryEntryId = entryId, FilePath = filePath });
        await _db.SaveChangesAsync();
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter DiaryEntryServiceTests
```

Expected: `통과!` / 6개 테스트 성공

- [ ] **Step 5: 커밋**

```bash
git add src/DiaryApp/Services/DiaryEntryService.cs tests/DiaryApp.Tests/DiaryEntryServiceTests.cs
git commit -m "Add DiaryEntryService for create/read/image attach"
```

---

## Task 4: RetrospectiveService (계단식 회고)

**Files:**
- Create: `src/DiaryApp/Services/RetrospectiveService.cs`
- Test: `tests/DiaryApp.Tests/RetrospectiveServiceTests.cs`

**Interfaces:**
- Consumes: `DiaryDbContext`, `DiaryEntry`(Task 2), `DiaryEntryService`(Task 3, 테스트 데이터 준비용), `TestDb`(Task 2)
- Produces:
  - `DiaryApp.Services.RetrospectiveTier(string Label, List<DiaryEntry> Entries)` (record)
  - `DiaryApp.Services.RetrospectiveService(DiaryDbContext db)` — `Task<List<RetrospectiveTier>> GetRetrospectiveAsync(DateTime referenceDate)`
  - Task 8(홈 화면)이 `GetRetrospectiveAsync(DateTime.Today)`를 호출해 `RetrospectiveTier` 목록을 렌더링한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/DiaryApp.Tests/RetrospectiveServiceTests.cs`:

```csharp
namespace DiaryApp.Tests;

using DiaryApp.Services;
using Xunit;

public class RetrospectiveServiceTests
{
    [Fact]
    public async Task ReturnsEntryFromSevenDaysAgo()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var retroService = new RetrospectiveService(db.Context);

        await entryService.CreateAsync(new DateTime(2026, 7, 13), "일주일 전 기록", null, new List<string>());

        var tiers = await retroService.GetRetrospectiveAsync(new DateTime(2026, 7, 20));

        var weekTier = Assert.Single(tiers, t => t.Label == "7일 전");
        Assert.Single(weekTier.Entries);
        Assert.Equal("일주일 전 기록", weekTier.Entries[0].Content);
    }

    [Fact]
    public async Task ReturnsEntryFromOneMonthAgo()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var retroService = new RetrospectiveService(db.Context);

        await entryService.CreateAsync(new DateTime(2026, 6, 20), "한달 전 기록", null, new List<string>());

        var tiers = await retroService.GetRetrospectiveAsync(new DateTime(2026, 7, 20));

        var monthTier = Assert.Single(tiers, t => t.Label == "1개월 전");
        Assert.Single(monthTier.Entries);
    }

    [Fact]
    public async Task ReturnsEntriesFromMultipleYearsAgoOrderedNewestFirst()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var retroService = new RetrospectiveService(db.Context);

        await entryService.CreateAsync(new DateTime(2024, 7, 20), "2년 전 기록", null, new List<string>());
        await entryService.CreateAsync(new DateTime(2025, 7, 20), "1년 전 기록", null, new List<string>());

        var tiers = await retroService.GetRetrospectiveAsync(new DateTime(2026, 7, 20));

        var labels = tiers.Select(t => t.Label).ToList();
        Assert.Equal(new[] { "1년 전", "2년 전" }, labels);
    }

    [Fact]
    public async Task SkipsTiersWithNoMatchingEntries()
    {
        using var db = new TestDb();
        var retroService = new RetrospectiveService(db.Context);

        var tiers = await retroService.GetRetrospectiveAsync(new DateTime(2026, 7, 20));

        Assert.Empty(tiers);
    }

    [Fact]
    public async Task HandlesLeapDayReferenceCorrectly()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var retroService = new RetrospectiveService(db.Context);

        await entryService.CreateAsync(new DateTime(2024, 2, 29), "윤년 기록", null, new List<string>());

        var tiers = await retroService.GetRetrospectiveAsync(new DateTime(2028, 2, 29));

        var yearTier = Assert.Single(tiers, t => t.Label == "4년 전");
        Assert.Single(yearTier.Entries);
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter RetrospectiveServiceTests
```

Expected: 컴파일 에러 (`DiaryApp.Services.RetrospectiveService` 타입 없음)

- [ ] **Step 3: 구현**

`src/DiaryApp/Services/RetrospectiveService.cs`:

```csharp
namespace DiaryApp.Services;

using DiaryApp.Data;
using DiaryApp.Models;
using Microsoft.EntityFrameworkCore;

public record RetrospectiveTier(string Label, List<DiaryEntry> Entries);

public class RetrospectiveService
{
    private readonly DiaryDbContext _db;

    public RetrospectiveService(DiaryDbContext db)
    {
        _db = db;
    }

    public async Task<List<RetrospectiveTier>> GetRetrospectiveAsync(DateTime referenceDate)
    {
        var tiers = new List<RetrospectiveTier>();

        var weekAgo = referenceDate.AddDays(-7);
        var weekAgoEntries = await _db.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .Where(e => e.Date.Year == weekAgo.Year && e.Date.Month == weekAgo.Month && e.Date.Day == weekAgo.Day)
            .ToListAsync();
        if (weekAgoEntries.Count > 0)
            tiers.Add(new RetrospectiveTier("7일 전", weekAgoEntries));

        var monthAgo = referenceDate.AddMonths(-1);
        var monthAgoEntries = await _db.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .Where(e => e.Date.Year == monthAgo.Year && e.Date.Month == monthAgo.Month && e.Date.Day == monthAgo.Day)
            .ToListAsync();
        if (monthAgoEntries.Count > 0)
            tiers.Add(new RetrospectiveTier("1개월 전", monthAgoEntries));

        var pastYearsEntries = await _db.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .Where(e => e.Date.Month == referenceDate.Month
                     && e.Date.Day == referenceDate.Day
                     && e.Date.Year < referenceDate.Year)
            .ToListAsync();

        foreach (var group in pastYearsEntries.GroupBy(e => e.Date.Year).OrderByDescending(g => g.Key))
        {
            var yearsAgo = referenceDate.Year - group.Key;
            tiers.Add(new RetrospectiveTier($"{yearsAgo}년 전", group.ToList()));
        }

        return tiers;
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter RetrospectiveServiceTests
```

Expected: `통과!` / 5개 테스트 성공

- [ ] **Step 5: 커밋**

```bash
git add src/DiaryApp/Services/RetrospectiveService.cs tests/DiaryApp.Tests/RetrospectiveServiceTests.cs
git commit -m "Add RetrospectiveService with 7-day/1-month/N-year tiers"
```

---

## Task 5: SearchService (텍스트·태그 검색)

**Files:**
- Create: `src/DiaryApp/Services/SearchService.cs`
- Test: `tests/DiaryApp.Tests/SearchServiceTests.cs`

**Interfaces:**
- Consumes: `DiaryDbContext`, `DiaryEntry`(Task 2), `DiaryEntryService`(Task 3, 테스트 데이터 준비용), `TestDb`(Task 2)
- Produces: `DiaryApp.Services.SearchService(DiaryDbContext db)` —
  - `Task<List<DiaryEntry>> SearchAsync(string? keyword, List<string>? tagNames)` (태그 여러 개는 OR)
  - `Task<List<string>> GetAllTagNamesAsync()`
  - Task 10(목록·검색 화면)이 이 두 메서드를 사용한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/DiaryApp.Tests/SearchServiceTests.cs`:

```csharp
namespace DiaryApp.Tests;

using DiaryApp.Services;
using Xunit;

public class SearchServiceTests
{
    [Fact]
    public async Task SearchAsync_FiltersByKeyword()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var searchService = new SearchService(db.Context);

        await entryService.CreateAsync(new DateTime(2026, 7, 20), "오늘은 카페에 갔다", null, new List<string>());
        await entryService.CreateAsync(new DateTime(2026, 7, 19), "집에서 쉬었다", null, new List<string>());

        var results = await searchService.SearchAsync("카페", null);

        Assert.Single(results);
        Assert.Equal("오늘은 카페에 갔다", results[0].Content);
    }

    [Fact]
    public async Task SearchAsync_FiltersByTagUsingOrLogic()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var searchService = new SearchService(db.Context);

        await entryService.CreateAsync(new DateTime(2026, 7, 20), "글1", null, new List<string> { "행복" });
        await entryService.CreateAsync(new DateTime(2026, 7, 19), "글2", null, new List<string> { "피곤" });
        await entryService.CreateAsync(new DateTime(2026, 7, 18), "글3", null, new List<string> { "슬픔" });

        var results = await searchService.SearchAsync(null, new List<string> { "행복", "피곤" });

        Assert.Equal(2, results.Count);
    }

    [Fact]
    public async Task SearchAsync_WithNoFilters_ReturnsAllOrderedByDateDescending()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var searchService = new SearchService(db.Context);

        await entryService.CreateAsync(new DateTime(2026, 7, 18), "오래된 글", null, new List<string>());
        await entryService.CreateAsync(new DateTime(2026, 7, 20), "최신 글", null, new List<string>());

        var results = await searchService.SearchAsync(null, null);

        Assert.Equal(2, results.Count);
        Assert.Equal("최신 글", results[0].Content);
    }

    [Fact]
    public async Task GetAllTagNamesAsync_ReturnsSortedNames()
    {
        using var db = new TestDb();
        var entryService = new DiaryEntryService(db.Context);
        var searchService = new SearchService(db.Context);

        await entryService.CreateAsync(new DateTime(2026, 7, 20), "글1", null, new List<string> { "행복", "일상" });
        await entryService.CreateAsync(new DateTime(2026, 7, 19), "글2", null, new List<string> { "일상" });

        var tags = await searchService.GetAllTagNamesAsync();

        Assert.Equal(new[] { "일상", "행복" }, tags);
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter SearchServiceTests
```

Expected: 컴파일 에러 (`DiaryApp.Services.SearchService` 타입 없음)

- [ ] **Step 3: 구현**

`src/DiaryApp/Services/SearchService.cs`:

```csharp
namespace DiaryApp.Services;

using DiaryApp.Data;
using DiaryApp.Models;
using Microsoft.EntityFrameworkCore;

public class SearchService
{
    private readonly DiaryDbContext _db;

    public SearchService(DiaryDbContext db)
    {
        _db = db;
    }

    public async Task<List<DiaryEntry>> SearchAsync(string? keyword, List<string>? tagNames)
    {
        var query = _db.DiaryEntries
            .Include(e => e.Tags)
            .Include(e => e.Images)
            .AsQueryable();

        if (!string.IsNullOrWhiteSpace(keyword))
        {
            query = query.Where(e => e.Content.Contains(keyword));
        }

        if (tagNames != null && tagNames.Count > 0)
        {
            query = query.Where(e => e.Tags.Any(t => tagNames.Contains(t.Name)));
        }

        return await query
            .OrderByDescending(e => e.Date)
            .ThenByDescending(e => e.CreatedAt)
            .ToListAsync();
    }

    public async Task<List<string>> GetAllTagNamesAsync()
    {
        return await _db.Tags.OrderBy(t => t.Name).Select(t => t.Name).ToListAsync();
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter SearchServiceTests
```

Expected: `통과!` / 4개 테스트 성공

- [ ] **Step 5: 커밋**

```bash
git add src/DiaryApp/Services/SearchService.cs tests/DiaryApp.Tests/SearchServiceTests.cs
git commit -m "Add SearchService for keyword and tag (OR) filtering"
```

---

## Task 6: ImageStorageService (이미지 저장/검증)

**Files:**
- Create: `src/DiaryApp/Services/ImageStorageService.cs`
- Test: `tests/DiaryApp.Tests/ImageStorageServiceTests.cs`

**Interfaces:**
- Consumes: 없음 (독립적인 파일 I/O 서비스)
- Produces:
  - `DiaryApp.Services.ImageValidationException(string message)`
  - `DiaryApp.Services.ImageStorageService(string uploadRoot)` — `Task<string> SaveAsync(Stream fileStream, string originalFileName, long fileSizeBytes)` (반환값은 `uploads/파일명` 형태의 상대 경로)
  - Task 7(DI 등록)이 `uploadRoot = wwwroot/uploads`로 이 서비스를 싱글턴 등록하고, Task 9(새 일기 작성 화면)이 `SaveAsync`를 호출한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/DiaryApp.Tests/ImageStorageServiceTests.cs`:

```csharp
namespace DiaryApp.Tests;

using DiaryApp.Services;
using Xunit;

public class ImageStorageServiceTests : IDisposable
{
    private readonly string _uploadRoot;

    public ImageStorageServiceTests()
    {
        _uploadRoot = Path.Combine(Path.GetTempPath(), "DiaryAppTests_" + Guid.NewGuid().ToString("N"));
    }

    public void Dispose()
    {
        if (Directory.Exists(_uploadRoot))
            Directory.Delete(_uploadRoot, recursive: true);
    }

    [Fact]
    public async Task SaveAsync_SavesFileAndReturnsRelativePath()
    {
        var service = new ImageStorageService(_uploadRoot);
        using var stream = new MemoryStream(new byte[] { 1, 2, 3, 4 });

        var relativePath = await service.SaveAsync(stream, "photo.jpg", stream.Length);

        Assert.StartsWith("uploads/", relativePath);
        var savedFileName = relativePath.Substring("uploads/".Length);
        Assert.True(File.Exists(Path.Combine(_uploadRoot, savedFileName)));
    }

    [Fact]
    public async Task SaveAsync_RejectsDisallowedExtension()
    {
        var service = new ImageStorageService(_uploadRoot);
        using var stream = new MemoryStream(new byte[] { 1, 2, 3 });

        await Assert.ThrowsAsync<ImageValidationException>(
            () => service.SaveAsync(stream, "document.pdf", stream.Length));
    }

    [Fact]
    public async Task SaveAsync_RejectsFileOverSizeLimit()
    {
        var service = new ImageStorageService(_uploadRoot);
        using var stream = new MemoryStream(new byte[10]);
        var overLimitSize = 5 * 1024 * 1024 + 1;

        await Assert.ThrowsAsync<ImageValidationException>(
            () => service.SaveAsync(stream, "big.jpg", overLimitSize));
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter ImageStorageServiceTests
```

Expected: 컴파일 에러 (`DiaryApp.Services.ImageStorageService`/`ImageValidationException` 타입 없음)

- [ ] **Step 3: 구현**

`src/DiaryApp/Services/ImageStorageService.cs`:

```csharp
namespace DiaryApp.Services;

public class ImageValidationException : Exception
{
    public ImageValidationException(string message) : base(message) { }
}

public class ImageStorageService
{
    private static readonly HashSet<string> AllowedExtensions = new(StringComparer.OrdinalIgnoreCase)
    {
        ".jpg", ".jpeg", ".png", ".gif", ".webp"
    };
    private const long MaxSizeBytes = 5 * 1024 * 1024;

    private readonly string _uploadRoot;

    public ImageStorageService(string uploadRoot)
    {
        _uploadRoot = uploadRoot;
    }

    public async Task<string> SaveAsync(Stream fileStream, string originalFileName, long fileSizeBytes)
    {
        var extension = Path.GetExtension(originalFileName);
        if (!AllowedExtensions.Contains(extension))
            throw new ImageValidationException($"지원하지 않는 이미지 형식입니다: {extension}");

        if (fileSizeBytes > MaxSizeBytes)
            throw new ImageValidationException("이미지 용량은 5MB를 초과할 수 없습니다.");

        Directory.CreateDirectory(_uploadRoot);

        var fileName = $"{Guid.NewGuid():N}{extension}";
        var fullPath = Path.Combine(_uploadRoot, fileName);

        using (var output = File.Create(fullPath))
        {
            await fileStream.CopyToAsync(output);
        }

        return $"uploads/{fileName}";
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
dotnet test tests/DiaryApp.Tests --filter ImageStorageServiceTests
```

Expected: `통과!` / 3개 테스트 성공

- [ ] **Step 5: 커밋**

```bash
git add src/DiaryApp/Services/ImageStorageService.cs tests/DiaryApp.Tests/ImageStorageServiceTests.cs
git commit -m "Add ImageStorageService with size/extension validation"
```

---

## Task 7: DI 등록 및 앱 시작 시 DB 초기화

**Files:**
- Modify: `src/DiaryApp/Program.cs` (전체 교체)

**Interfaces:**
- Consumes: `DiaryDbContext`(Task 2), `DiaryEntryService`(Task 3), `RetrospectiveService`(Task 4), `SearchService`(Task 5), `ImageStorageService`(Task 6)
- Produces: DI 컨테이너에 위 4개 서비스가 등록되고, 앱 시작 시 `diary.db`가 없으면 자동 생성됨. Task 8~10의 Razor 페이지들이 `@inject`로 이 서비스들을 사용할 수 있게 된다.

- [ ] **Step 1: `Program.cs` 전체 교체**

`src/DiaryApp/Program.cs`:

```csharp
using DiaryApp.Components;
using DiaryApp.Data;
using DiaryApp.Services;
using Microsoft.EntityFrameworkCore;

var builder = WebApplication.CreateBuilder(args);

// Add services to the container.
builder.Services.AddRazorComponents()
    .AddInteractiveServerComponents();

var dbPath = Path.Combine(builder.Environment.ContentRootPath, "diary.db");
builder.Services.AddDbContext<DiaryDbContext>(options =>
    options.UseSqlite($"Data Source={dbPath}"));

var uploadRoot = Path.Combine(builder.Environment.WebRootPath, "uploads");
builder.Services.AddScoped<DiaryEntryService>();
builder.Services.AddScoped<RetrospectiveService>();
builder.Services.AddScoped<SearchService>();
builder.Services.AddSingleton(new ImageStorageService(uploadRoot));

var app = builder.Build();

using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<DiaryDbContext>();
    db.Database.EnsureCreated();
}

// Configure the HTTP request pipeline.
if (!app.Environment.IsDevelopment())
{
    app.UseExceptionHandler("/Error", createScopeForErrors: true);
    // The default HSTS value is 30 days. You may want to change this for production scenarios, see https://aka.ms/aspnetcore-hsts.
    app.UseHsts();
}
app.UseStatusCodePagesWithReExecute("/not-found", createScopeForStatusCodePages: true);
app.UseHttpsRedirection();

app.UseAntiforgery();

app.MapStaticAssets();
app.MapRazorComponents<App>()
    .AddInteractiveServerRenderMode();

app.Run();
```

- [ ] **Step 2: 빌드 및 기동 확인**

```bash
dotnet build DiaryApp.slnx
```

Expected: 오류 0개

```bash
dotnet run --project src/DiaryApp --urls http://localhost:5299 &
sleep 3
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5299/
ls src/DiaryApp/diary.db
kill %1
```

Expected: `200` 출력, `diary.db` 파일이 `src/DiaryApp/` 아래 생성되어 있음

- [ ] **Step 3: 커밋**

```bash
git add src/DiaryApp/Program.cs
git commit -m "Wire up EF Core DbContext and domain services via DI"
```

---

## Task 8: 홈 화면 — 회고

**Files:**
- Modify: `src/DiaryApp/Components/Pages/Home.razor` (전체 교체)
- Modify: `src/DiaryApp/Components/_Imports.razor` (using 추가)

**Interfaces:**
- Consumes: `RetrospectiveService.GetRetrospectiveAsync(DateTime)`(Task 4), `RetrospectiveTier`(Task 4)
- Produces: `/` 경로에서 회고 화면이 렌더링됨. `_Imports.razor`에 추가되는 `@using DiaryApp.Models`, `@using DiaryApp.Services`는 Task 9, 10에서도 재사용된다.

- [ ] **Step 1: `_Imports.razor`에 네임스페이스 추가**

`src/DiaryApp/Components/_Imports.razor` 끝에 두 줄 추가:

```razor
@using DiaryApp.Models
@using DiaryApp.Services
```

- [ ] **Step 2: `Home.razor` 전체 교체**

`src/DiaryApp/Components/Pages/Home.razor`:

```razor
@page "/"
@inject RetrospectiveService RetroService

<PageTitle>회고</PageTitle>

<h1>회고</h1>

@if (tiers is null)
{
    <p>불러오는 중...</p>
}
else if (tiers.Count == 0)
{
    <p>오늘 기준으로 회고할 기록이 아직 없어요.</p>
}
else
{
    @foreach (var tier in tiers)
    {
        <section>
            <h2>@tier.Label</h2>
            @foreach (var entry in tier.Entries)
            {
                <article>
                    <p>@entry.Date.ToString("yyyy-MM-dd")</p>
                    <p>@entry.Content</p>
                    @if (entry.Tags.Count > 0)
                    {
                        <p>@string.Join(", ", entry.Tags.Select(t => t.Name))</p>
                    }
                </article>
            }
        </section>
    }
}

@code {
    private List<RetrospectiveTier>? tiers;

    protected override async Task OnInitializedAsync()
    {
        tiers = await RetroService.GetRetrospectiveAsync(DateTime.Today);
    }
}
```

- [ ] **Step 3: 수동 확인**

```bash
dotnet run --project src/DiaryApp --urls http://localhost:5299 &
sleep 3
curl -s http://localhost:5299/ | grep -o "회고할 기록이 아직 없어요"
kill %1
```

Expected: `회고할 기록이 아직 없어요` 출력 (아직 데이터가 없으므로 빈 상태 메시지가 보이는 것이 정상)

- [ ] **Step 4: 커밋**

```bash
git add src/DiaryApp/Components/Pages/Home.razor src/DiaryApp/Components/_Imports.razor
git commit -m "Add retrospective home page"
```

---

## Task 9: 새 일기 작성 화면

**Files:**
- Create: `src/DiaryApp/Components/Pages/NewEntry.razor`

**Interfaces:**
- Consumes: `DiaryEntryService.CreateAsync`/`AddImageAsync`(Task 3), `ImageStorageService.SaveAsync`(Task 6), `_Imports.razor`의 `@using DiaryApp.Models`/`@using DiaryApp.Services`(Task 8)
- Produces: `/new` 경로에서 작성 폼이 렌더링되고, 제출 시 `/timeline`으로 이동. Task 11에서 이 화면을 통해 수동 테스트 데이터를 만든다.

- [ ] **Step 1: `NewEntry.razor` 작성**

`src/DiaryApp/Components/Pages/NewEntry.razor`:

```razor
@page "/new"
@using System.ComponentModel.DataAnnotations
@inject DiaryEntryService EntryService
@inject ImageStorageService ImageService
@inject NavigationManager Nav

<PageTitle>새 일기</PageTitle>

<h1>새 일기 작성</h1>

<EditForm Model="model" OnValidSubmit="HandleSubmit">
    <DataAnnotationsValidator />

    <div>
        <label>날짜</label>
        <InputDate @bind-Value="model.Date" />
    </div>

    <div>
        <label>본문</label>
        <InputTextArea @bind-Value="model.Content" />
        <ValidationMessage For="() => model.Content" />
    </div>

    <div>
        <label>태그 (쉼표로 구분)</label>
        <InputText @bind-Value="model.TagsRaw" />
    </div>

    <div>
        <label>기분</label>
        <InputSelect @bind-Value="model.Mood">
            <option value="">선택 안 함</option>
            <option value="1">😞 1</option>
            <option value="2">🙁 2</option>
            <option value="3">😐 3</option>
            <option value="4">🙂 4</option>
            <option value="5">😄 5</option>
        </InputSelect>
    </div>

    <div>
        <label>이미지 첨부</label>
        <InputFile OnChange="HandleFileSelection" multiple />
    </div>

    @if (!string.IsNullOrEmpty(errorMessage))
    {
        <p class="error">@errorMessage</p>
    }

    <button type="submit">저장</button>
</EditForm>

@code {
    private NewEntryModel model = new() { Date = DateTime.Today };
    private List<IBrowserFile> selectedFiles = new();
    private string? errorMessage;

    private void HandleFileSelection(InputFileChangeEventArgs e)
    {
        selectedFiles = e.GetMultipleFiles(maximumFileCount: 10).ToList();
    }

    private async Task HandleSubmit()
    {
        errorMessage = null;
        try
        {
            var tagNames = model.TagsRaw
                .Split(',', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries)
                .ToList();

            var entry = await EntryService.CreateAsync(model.Date, model.Content, model.Mood, tagNames);

            foreach (var file in selectedFiles)
            {
                using var stream = file.OpenReadStream(maxAllowedSize: 5 * 1024 * 1024);
                var relativePath = await ImageService.SaveAsync(stream, file.Name, file.Size);
                await EntryService.AddImageAsync(entry.Id, relativePath);
            }

            Nav.NavigateTo("/timeline");
        }
        catch (Exception ex)
        {
            errorMessage = ex.Message;
        }
    }

    private class NewEntryModel
    {
        public DateTime Date { get; set; }

        [Required(ErrorMessage = "본문을 입력해주세요.")]
        public string Content { get; set; } = string.Empty;

        public string TagsRaw { get; set; } = string.Empty;

        public int? Mood { get; set; }
    }
}
```

- [ ] **Step 2: 빌드 확인**

```bash
dotnet build DiaryApp.slnx
```

Expected: 오류 0개

- [ ] **Step 3: 커밋**

```bash
git add src/DiaryApp/Components/Pages/NewEntry.razor
git commit -m "Add new entry form page"
```

---

## Task 10: 목록·검색 화면

**Files:**
- Create: `src/DiaryApp/Components/Pages/Timeline.razor`

**Interfaces:**
- Consumes: `DiaryEntryService.GetAllDescendingAsync`(Task 3), `SearchService.SearchAsync`/`GetAllTagNamesAsync`(Task 5)
- Produces: `/timeline` 경로에서 목록이 최신순으로 보이고, 텍스트 입력·태그 체크박스로 필터링됨.

- [ ] **Step 1: `Timeline.razor` 작성**

`src/DiaryApp/Components/Pages/Timeline.razor`:

```razor
@page "/timeline"
@inject DiaryEntryService EntryService
@inject SearchService SearchService

<PageTitle>목록·검색</PageTitle>

<h1>일기 목록</h1>

<div>
    <input type="text" placeholder="검색어" @bind="keyword" @bind:event="oninput" @onkeyup="RefreshResults" />
</div>

<div>
    @foreach (var tag in allTags)
    {
        <label>
            <input type="checkbox" checked="@selectedTags.Contains(tag)"
                   @onchange="@(e => ToggleTag(tag, (bool)e.Value!))" />
            @tag
        </label>
    }
</div>

@if (entries is null)
{
    <p>불러오는 중...</p>
}
else if (entries.Count == 0)
{
    <p>표시할 일기가 없어요.</p>
}
else
{
    @foreach (var entry in entries)
    {
        <article>
            <p>@entry.Date.ToString("yyyy-MM-dd")</p>
            <p>@entry.Content</p>
            @if (entry.Tags.Count > 0)
            {
                <p>@string.Join(", ", entry.Tags.Select(t => t.Name))</p>
            }
            @if (entry.Images.Count > 0)
            {
                <div>
                    @foreach (var image in entry.Images)
                    {
                        <img src="@image.FilePath" alt="첨부 이미지" width="120" />
                    }
                </div>
            }
        </article>
    }
}

@code {
    private List<DiaryEntry>? entries;
    private List<string> allTags = new();
    private HashSet<string> selectedTags = new();
    private string keyword = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        allTags = await SearchService.GetAllTagNamesAsync();
        entries = await EntryService.GetAllDescendingAsync();
    }

    private async Task ToggleTag(string tag, bool isChecked)
    {
        if (isChecked)
            selectedTags.Add(tag);
        else
            selectedTags.Remove(tag);
        await RefreshResults();
    }

    private async Task RefreshResults()
    {
        var tags = selectedTags.Count > 0 ? selectedTags.ToList() : null;
        var keywordFilter = string.IsNullOrWhiteSpace(keyword) ? null : keyword;
        entries = await SearchService.SearchAsync(keywordFilter, tags);
    }
}
```

- [ ] **Step 2: 빌드 확인**

```bash
dotnet build DiaryApp.slnx
```

Expected: 오류 0개

- [ ] **Step 3: 커밋**

```bash
git add src/DiaryApp/Components/Pages/Timeline.razor
git commit -m "Add timeline page with keyword and tag search"
```

---

## Task 11: 내비게이션 연결 + 전체 수동 검증

**Files:**
- Modify: `src/DiaryApp/Components/Layout/MainLayout.razor`

**Interfaces:**
- Consumes: Task 8/9/10에서 만든 `/`, `/new`, `/timeline` 라우트
- Produces: 세 화면 사이를 이동할 수 있는 완성된 앱

- [ ] **Step 1: `MainLayout.razor`에 내비게이션 추가**

`src/DiaryApp/Components/Layout/MainLayout.razor`:

```razor
@inherits LayoutComponentBase

<nav>
    <a href="/">회고</a> |
    <a href="/new">새 일기</a> |
    <a href="/timeline">목록·검색</a>
</nav>

@Body

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
```

- [ ] **Step 2: 전체 자동 테스트 실행**

```bash
dotnet test DiaryApp.slnx
```

Expected: 지금까지 작성한 모든 테스트(2+6+5+4+3=20개) 통과, 실패 0

- [ ] **Step 3: 수동 end-to-end 검증**

```bash
dotnet run --project src/DiaryApp --urls http://localhost:5299
```

브라우저(또는 Claude in Chrome)로 `http://localhost:5299` 접속 후 아래를 순서대로 확인:

1. `/timeline`에서 "새 일기" 링크로 `/new` 이동
2. 날짜를 **오늘로부터 7일 전**으로 지정, 본문 입력, 태그 `테스트` 입력, 기분 선택, 이미지 1장 첨부 후 저장 → `/timeline`으로 이동하며 방금 쓴 글이 목록 맨 위에 보이는지 확인
3. `/timeline`에서 검색어에 방금 쓴 본문의 일부 키워드를 입력해 필터링되는지 확인
4. `/timeline`에서 `테스트` 태그 체크박스를 눌러 태그 필터가 동작하는지 확인
5. `/`(홈)로 이동해 "7일 전" 섹션에 방금 쓴 글이 나타나는지 확인
6. 이미지가 목록/회고 화면에 깨지지 않고 표시되는지 확인

문제가 있으면 해당 Task로 돌아가 수정한다. 정상 동작하면 서버 종료(Ctrl+C).

- [ ] **Step 4: 커밋**

```bash
git add src/DiaryApp/Components/Layout/MainLayout.razor
git commit -m "Wire up navigation between home, new entry, and timeline pages"
```
